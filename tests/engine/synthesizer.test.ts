import { describe, it } from "vitest";
// import { synthesize } from "../../src/engine/synthesizer.js";

describe("synthesizer", () => {
  it("TODO: parses judge JSON and applies fallbacks when malformed", () => {
    // TODO: Mock LlmClient returning valid JSON vs markdown-wrapped JSON vs invalid text.
    // TODO: Assert synthesis shape, disagreements, and consensusPoints.
    throw new Error("Not implemented");
  });
});
